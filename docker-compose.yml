version: '3'

services:
    db:
        image: postgres
    web:
        build: .
        command: npm start
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        ports:
            - "8080:8080"
        depends_on:
            - db
